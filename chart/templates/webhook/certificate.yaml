{{- if and .Values.webhook.enabled .Values.certManager.enabled }}
---
# Self-signed Issuer for webhook certificates
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ .Values.webhook.certificate.issuerName }}
  namespace: {{ include "lynq-operator.namespace" . }}
  labels:
    {{- include "lynq-operator.labels" . | nindent 4 }}
spec:
  selfSigned: {}
---
# Certificate for webhook server
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "lynq-operator.webhookCertificateName" . }}
  namespace: {{ include "lynq-operator.namespace" . }}
  labels:
    {{- include "lynq-operator.labels" . | nindent 4 }}
    app.kubernetes.io/component: webhook
spec:
  dnsNames:
  - {{ include "lynq-operator.webhookServiceName" . }}.{{ include "lynq-operator.namespace" . }}.svc
  - {{ include "lynq-operator.webhookServiceName" . }}.{{ include "lynq-operator.namespace" . }}.svc.cluster.local
  issuerRef:
    kind: {{ .Values.webhook.certificate.issuerKind }}
    name: {{ .Values.webhook.certificate.issuerName }}
  secretName: {{ include "lynq-operator.webhookCertificateName" . }}
  {{- if .Values.webhook.certificate.duration }}
  duration: {{ .Values.webhook.certificate.duration }}
  {{- end }}
  {{- if .Values.webhook.certificate.renewBefore }}
  renewBefore: {{ .Values.webhook.certificate.renewBefore }}
  {{- end }}
{{- end }}
